#include <stdio.h>
#include <stdbool.h>
#include <math.h>

#include "triangleAngle.h"

int* triangleAngle(int side1, int side2, int side3)
{
	int* angle1;
	int* angle2;
	int* angle3;
	float pi;
	pi = acos(-1);

	while ((side1 <= 0 || side2 <= 0 || side3 <= 0))
	{

	}
}